import os

version = "rustup_wrap"
car_deps = []
description = "Rust programming language compiler and tools"

def install():
  print(":: Running rustup")
  os.system("curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh")
